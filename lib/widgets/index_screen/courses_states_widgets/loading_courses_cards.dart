import 'package:flutter/cupertino.dart';

class LoadingCoursesCards extends StatelessWidget {
  const LoadingCoursesCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}